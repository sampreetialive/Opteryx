{
  "name": "scamshield",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ScamShield scam-awareness web app",
  "scripts": {
    "check": "node --check app.js && node --check auth.js && node --check aws/strands-agent/src/agent.ts && node --check aws/strands-agent/src/tools.ts"
  }
}
